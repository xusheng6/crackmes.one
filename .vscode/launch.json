{
    // Keeping it in repository for easy debug 
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug crackmes.one",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "env": {},
            "args": [],
            "cwd": "${workspaceFolder}",
            "buildFlags": "-v"
        }
    ]
}
